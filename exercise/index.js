// const {currentDate, currentYear} = require ("./logger");

// console.log(currentDate(), currentYear());   
// OR

const logger = require ("./logger");
console.log(logger.currentDate(), logger.currentYear());   